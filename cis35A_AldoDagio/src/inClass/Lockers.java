package inClass;

import java.util.Scanner;

public class Lockers 
{
	/*
	public static void main(String[] args) 
	{
		
	
	boolean[] closed = new boolean[100];
	boolean[] student = new boolean[100];
	
		for(int i = 0; i < closed.length; i++)
		{
			boolean check = true;
			student[0] = check;
			// check if locker is 
			if(closed[i] != check)
				closed[i] = student[i];
			else if(closed[i] == check)
				
				System.out.println("Locker [" + i + "] is closed still.");
			
		}
		
		System.out.println("Lockers are now all open.");
	}
		*/

}
